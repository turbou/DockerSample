apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agents-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 4Gi
  storageClassName: hostpath

