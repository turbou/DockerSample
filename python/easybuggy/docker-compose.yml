version: '3'

services:
  django:
    build:
      context: .
    container_name: easybuggy
    restart: always
    command: python /project/easybuggy/manage.py runserver 0.0.0.0:8080
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true

